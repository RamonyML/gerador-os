node_modules
.firebase
*-debug.log
.env
.env.local
web/.env
web/.env.local
# functions/.env contém apenas variáveis não-sensíveis (MK_BASE_URL, MK_MODE)
# Os secrets reais (MK_TOKEN, MK_WEBSERVICE_PASSWORD) ficam no Secret Manager (prod) ou .secret.local (emulador)
!functions/.env
functions/.secret.local
web/dist
dist
!ceps/dist
functions/lib
functions/node_modules
.DS_Store
Thumbs.db
