node_modules
.firebase
*-debug.log
.env
.env.local
web/.env
web/.env.local
# functions/.env contém apenas variáveis não-sensíveis (MK_BASE_URL, MK_MODE)
# Os secrets reais (MK_TOKEN, MK_WEBSERVICE_PASSWORD) ficam no Secret Manager
!functions/.env
web/dist
dist
!ceps/dist
functions/lib
functions/node_modules
.DS_Store
Thumbs.db
